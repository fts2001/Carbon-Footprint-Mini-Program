# 测试需求文档 - 碳行家小程序 (Carbon Footprint Mini Program)

本文档列出了验证碳行家小程序功能的全面测试需求。旨在供测试人员 (QA) 使用，以确保系统按预期工作。

## 1. 注册与认证 (Onboarding & Authentication)

### 1.1 登录流程
- **前置条件**: 用户未登录。
- **测试步骤**:
    1. 打开小程序。
    2. 验证是否重定向到 `pages/login/login` (如未授权)。
    3. 点击 "登录/注册"。
    4. 填写必要的个人信息 (昵称, 性别, 年龄等)。
    5. 提交表单。
- **预期结果**:
    - 用户登录成功。
    - 用户信息保存到数据库 (`userInfo` 集合)。
    - 用户重定向到 `信息中心` 或 `首页`。
    - *首次登录用户*: 应触发 "新用户奖励" 流程 (检查是否有红包弹窗)。

### 1.2 权限管理
- **测试步骤**:
    1. 在登录或首次尝试记录行程时，观察权限请求。
- **预期结果**:
    - 小程序请求 `用户位置 (User Location)` 权限。
    - 小程序请求 `微信运动 (WeRun)` 权限。
    - 小程序请求 `订阅消息` 权限。
    - *拒绝权限的情况*: 拒绝权限 -> 验证小程序显示 "需要授权" 提示框或功能受限 (例如只能手动输入行程)。

## 2. 核心功能：碳足迹追踪 (Core Feature: Tracking)

### 2.1 自动追踪 (标准流程)
- **前置条件**: 已授予 位置信息 和 微信运动 权限。
- **测试步骤**:
    1. 进入 `首页 (Home)`。
    2. 点击 "开始记录"。
    3. 移动 (步行、驾车等) 至少 3 分钟且距离大于 3 公里 (触发有效的奖励逻辑)。
    4. 点击 "结束行程"。
    5. 选择交通方式 (如未自动检测到) 和 出行目的。
- **预期结果**:
    - 计时器正常启动/停止。
    - 开始时调用 `createTrack`。
    - 结束时调用 `endTrack`。
    - 显示行程摘要 (距离, 节省碳量, 速度分析)。
    - 奖励积分 (如果符合条件)。

### 2.2 手动录入 (降级流程)
- **前置条件**: 拒绝位置权限 或 用户主动选择手动模式。
- **测试步骤**:
    1. 开始记录 (系统检测到无权限)。
    2. 结束记录。
    3. 验证出现 "手动录入" 弹窗。
    4. 手动输入: 距离, 交通方式, 目的。
- **预期结果**:
    - 行程基于手动输入的数据成功记录。
    - 碳节省量基于手动输入的距离计算。

### 2.3 行程历史
- **测试步骤**:
    1. 进入 `个人中心` -> `行程历史` (或类似入口)。
    2. 验证刚刚记录的行程出现在列表中。
    3. 点击行程查看详情。
- **预期结果**:
    - 详情与记录的行程一致 (日期, 距离, 碳量)。

## 3. 积分与奖励系统 (Points & Rewards)

### 3.1 每日积分
- **测试步骤**:
    1. 完成一次有效行程 (>3km, >3mins)。
    2. 检查 `每日积分 (Daily Credit)` 余额。
- **预期结果**:
    - 积分增加设定值 (例如 25 分)。
    - *上限检查*: 验证每日上限 (例如每天最多 50 分)。

### 3.2 抽奖 (Lottery)
- **测试步骤**:
    1. 进入 `奖励中心` -> `抽奖 (Lottery)` (积分抽奖)。
    2. 确保有足够积分 (例如 200 分)。
    3. 点击抽奖。
- **预期结果**:
    - 播放动画。
    - 显示结果 (中奖/未中奖)。
    - 积分立即扣除 (`lottery` 集合更新)。
    - 如中奖: 奖品出现在 `我的奖品` 中。

### 3.3 实物兑换 (Merchandise Exchange)
- **测试步骤**:
    1. 进入 `奖励中心`。
    2. 选择一个实物商品 (例如 帆布袋)。
    3. 点击 "兑换"。
- **预期结果**:
    - 积分扣除。
    - 显示成功提示。
    - 商品出现在 `我的奖品` 中。
    - *库存检查*: 如果商品库存不足，兑换应失败。

### 3.4 现金兑换 (红包)
- **测试步骤**:
    1. 进入 `奖励中心`。
    2. 选择 "现金奖励" (例如 0.3 元)。
    3. 点击 "兑换"。
- **预期结果**:
    - 积分扣除。
    - 跳转到红包页面 (Webview/Page)。
    - 通过外部 API 成功生成 Ticket。
    - *异常情况*: 如果外部 API 失败，积分应退还 (检查数据库)。

## 4. 用户资料与设置 (Profile & Settings)

### 4.1 更新资料
- **测试步骤**:
    1. 进入 `个人中心` -> `编辑资料`。
    2. 修改头像或昵称。
- **预期结果**:
    - 更改反映在 `userInfo` 集合中。
    - UI 立即更新。

### 4.2 周榜 (Weekly Ranking)
- **测试步骤**:
    1. 进入 `首页` -> `排行榜`。
- **预期结果**:
    - 验证显示用户的排名 (基于总省碳量)。
    - 加载前几名用户列表。

## 5. 边界情况与稳定性 (Edge Cases)

- **网络故障**: 在 `结束行程` 时模拟断网。
    - *预期*: App 应优雅处理 (重试或本地保存稍后同步 - 至少要有错误提示)。
- **积分不足**: 尝试用 0 积分兑换商品。
    - *预期*: 错误提示 "积分不足"。
- **定位漂移**: (模拟) GPS 点跳变。
    - *预期*: 服务端 `endTrack` 逻辑应过滤不合理的速度 (根据 `speedBetween` 逻辑)。

## 6. 测试环境准备
- **数据库**: 确保 `config` 集合已设置 `lotteryQuantity`。
- **商品**: 确保 `merch` 集合中有库存 > 0 的商品。
- **用户**: 使用新的 `openid` 或清除数据库相关条目以测试 "新用户" 流程。
